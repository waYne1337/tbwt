#define compressors (each compressor must be available per seperate .sh - file in cp-directory)

#list only a couple of compressors:
#COMPRESSORS=bwz tbwz bcm tbcm wt twt

#list all available compressors:
COMPRESSORS=$(basename $(shell ls cp))
